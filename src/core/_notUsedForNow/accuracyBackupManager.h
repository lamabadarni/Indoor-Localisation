/*

// Created by halam on 24/05/2025.

#ifndef ACCURACY_DATA_MANAGER_H
#define ACCURACY_DATA_MANAGER_H

#include <vector>
#include <Core/utils/utilities.h>  
#include <scan_data_manager.h>
#include <sdCardBackup.h>


/*
/**
 * @brief Write all in-memory accuracy records to CSV.
 * @return true on success, false on failure opening/writing the file.
 */

//bool saveAllAccuracyRecords();

/**
 * @brief Load all accuracy records from CSV into memory.
 * @return true if file existed and was parsed, false otherwise.
 */
//bool loadAllAccuracyRecords();

/**
 * @brief Append a single AccuracyData row to the CSV (with header if empty).
 * @param row  The data to write.
 * @return true on success, false if unable to open file.
 */
//bool saveAccuracyData(const AccuracyData &row);

